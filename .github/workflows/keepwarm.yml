name: keep-warm
on:
  schedule: [{ cron: "*/5 * * * *" }]
  workflow_dispatch: {}
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -sS -m 30 https://clarie-web-mah2.vercel.app/api/health || true
